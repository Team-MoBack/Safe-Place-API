spring:
  datasource:
    password: root
    #TEST
    #url: jdbc:mysql://localhost:3306/moBack?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    #DEPLOY
    url: jdbc:mysql://safe-place-db:3306/moBack?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root

logging:
  level:
    org:
      springframework:
        web: DEBUG
      hibernate: ERROR